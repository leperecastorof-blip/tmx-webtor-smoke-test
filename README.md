# Webtor isolated validation

Ephemeral Docker tests only. Standard Ubuntu GitHub-hosted runner in a public repository. No paid runners, no artifacts/cache/packages published, no production secrets or customer data. No permanent hosting. Runtime outcomes are not assumed before tests execute.
